import React,{useState}from'react';import{useNavigate,Link}from'react-router-dom';import{api}from'../api';import{Button}from'../components/UI';
export default function Auth({register=false}){const n=useNavigate();const[f,setF]=useState({name:'',email:'',password:''});const[e,setE]=useState('');
const submit=async ev=>{ev.preventDefault();setE('');try{const d=await api(register?'/auth/register':'/auth/login',{method:'POST',body:JSON.stringify(f)});localStorage.setItem('token',d.token);localStorage.setItem('user',JSON.stringify(d.user));n('/home')}catch(x){setE(x.message)}};
return <div className="phone auth"><div className="authbox"><img src="/nirapod-poth-logo.jpeg" className="auth-logo"/><h1>{register?'Create Account':'Welcome Back'}</h1><p>{register?'Join Nirapod Poth':'Sign in to continue safely'}</p><form onSubmit={submit}>{register&&<input placeholder="Name" value={f.name} onChange={e=>setF({...f,name:e.target.value})}/>}<input type="email" required placeholder="Email" value={f.email} onChange={e=>setF({...f,email:e.target.value})}/><input type="password" required minLength="6" placeholder="Password" value={f.password} onChange={e=>setF({...f,password:e.target.value})}/>{e&&<div className="error">{e}</div>}<Button type="submit">{register?'Create Account':'Sign In'}</Button></form><p className="authswitch">{register?'Already have an account? ':'New here? '}<Link to={register?'/login':'/register'}>{register?'Sign In':'Create Account'}</Link></p></div></div>}